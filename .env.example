# Database Configuration
POSTGRES_USER=myform
POSTGRES_PASSWORD=change-me-to-a-secure-password
POSTGRES_DB=myform_db
DB_PORT=5432

# Server Configuration
NODE_ENV=production
SERVER_PORT=3001

# IMPORTANT: ADMIN_PASSWORD must be a bcrypt hash, not plaintext!
# Generate with: node -e "console.log(require('bcrypt').hashSync('your-password', 12))"
ADMIN_PASSWORD=change-me-to-bcrypt-hash

# Client Configuration
CLIENT_PORT=3000
CLIENT_URL=http://localhost
VITE_API_URL=/api
